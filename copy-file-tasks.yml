---
- name: Create dst directory if doesn't exist
  file:
    path: "{{item.dstPath}}"
    state: directory
    mode: 0755
    recurse: yes

- name: Copy file
  copy:
    src: "{{item.src}}"
    dest: "{{item.dstPath}}/{{item.dstName}}"
